name: isolate_executor
description: A library for executing closures on a separate isolate.
version: 1.0.0
homepage: https://github.com/stablekernel/isolate-executor
author: stable|kernel <jobs@stablekernel.com>

environment:
  sdk: '>=1.20.1 <2.0.0'

dependencies:
  analyzer: '>=0.30.0 <0.32.0'

dev_dependencies:
  test: ^0.12.0
  test_package:
    path: test/test_package
